class List {

    // constructor(list) {
    //     this.id = list.id
    //     this.name = list.name
    //     this.items = list.items ? list.items : []
    // }
    // 
    // renderListName() {
    //     return `<li class="lists-list" data-id=${this.id}>${this.name}</li>`
    // }
}
